# Notebooks

The first reproducible run is script-first so that data acquisition and preprocessing can be executed without hidden notebook state. Notebooks may be added later for interactive exploration of the generated `.h5ad` and result tables.
